spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  application:
    name: app
  output:
    ansi:
      enabled: always

  profiles:
    default: development

rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem